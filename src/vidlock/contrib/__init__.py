"""Ready-made task wrappers for sealing in the background.

Pick the one your project runs and point ``VIDLOCK['ENQUEUE_SEAL']`` at its
``enqueue``; queue a new upload with ``vidlock.pipeline.seal_later``.
"""
